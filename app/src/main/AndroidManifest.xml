<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthconnect2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LandingScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PatientSigninActivity"></activity>
        <activity android:name=".PatientHomeActivity"></activity>
        <activity android:name=".PatientSearchMainActivity"></activity>
        <activity android:name=".PatientSelectDeptActivity"></activity>
        <activity android:name=".PatientSelectDoctorActivity"></activity>
        <activity android:name=".PatientSelectSlotActivity"></activity>
        <activity android:name=".PatientBookConfirmedActivity"></activity>
        <activity android:name=".PatientSignupActivity"></activity>
        <activity android:name=".PatientProfileActivity"></activity>
        <activity android:name= ".ProviderSigninActivity"></activity>
    </application>

</manifest>